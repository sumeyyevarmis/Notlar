#include "main.h"

int main(){

	RCC->APB2ENR = (1 << 2);

	GPIOA->CRL &= ~(0xF << 28); // PA7
	GPIOA->CRL &= ~(0xF << 20); // PA5

	GPIOA->CRL |= (0b0010 << 28); // Output
	GPIOA->CRL |= (0b1000 << 20); // input

	while(1){

	}
}
