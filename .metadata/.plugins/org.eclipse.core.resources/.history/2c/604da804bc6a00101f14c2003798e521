#include "main.h"

int main(){

	// GPIOB clock enable
	RCC->APB2ENR |= (1 << 3);

	// GPIOB Port 3 Output
	GPIOB->CRL &= ~(0xF << 12);
	GPIOB->CRL |= (0b0011 << 12);

	while(1){
		GPIOB->BSRR |= (1 << 3);
		HAL_Delay(1000);
		GPIOB->BRR |= (1 << 3);
		HAL_Delay(1000);
	}
}
